/// <reference types="Cypress" />

describe("Search for specific Assests", () => {
  describe("when typing an asset name", () => {
    it("should be filtered the ones that match with the searched word", () => {});
  });

  describe("when typing an invalid asset name", () => {
    it("should be displayed an empty state", () => {});
  });
});
